apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-nlp-classifier-score-en
spec:
  selector:
    matchLabels:
      app: service-nlp-classifier-score-en
  replicas: 1
  template:
    metadata:
      labels:
        app: service-nlp-classifier-score-en
    spec:
      containers:
        - name: service-nlp-classifier-score-en
          image: cognigydevelopment.azurecr.io/service-nlp-classifier-en:2c096224e1caf0fb96f2e4452b09640146f31489
          resources:
            requests:
              cpu: '0.350'
              memory: 960M
            limits:
              cpu: '1.0'
              memory: 960M
          ports:
            - name: metrics
              containerPort: 8002
          volumeMounts:
            - name: rabbitmq-connection-string
              mountPath: /var/run/secrets/rabbitmqConnectionString
              subPath: rabbitmqConnectionString
            - name: mongodb-connection-string
              mountPath: /var/run/secrets/mongodbConnectionString
              subPath: mongodbConnectionString
            - name: local
              mountPath: /data/local
            - name: redis-password
              mountPath: /var/run/secrets/redis-password.conf
              subPath: redis-password.conf
          startupProbe:
            httpGet:
              path: /health/startup
              port: 8001
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8001
            failureThreshold: 1
            initialDelaySeconds: 120
            periodSeconds: 60
            timeoutSeconds: 60
          envFrom:
            - configMapRef:
                name: cognigy-env
          env:
            - name: LANGUAGES_TO_RUN
              value: en-US,en-AU,en-CA,en-IN,en-GB
            - name: SCORE
              value: 'true'
            - name: DEFAULT_EMBEDDING_MODEL
              value: usev4
      volumes:
        - name: rabbitmq-connection-string
          secret:
            secretName: cognigy-rabbitmq
            items:
              - key: connection-string
                path: rabbitmqConnectionString
        - name: mongodb-connection-string
          secret:
            secretName: cognigy-service-nlp
            items:
              - key: connection-string
                path: mongodbConnectionString
        - name: local
          emptyDir: {}
        - name: redis-password
          secret:
            secretName: cognigy-redis-password
      imagePullSecrets:
        - name: cognigy-registry-token
