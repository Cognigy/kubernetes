apiVersion: v1
kind: Secret
metadata:
    name: cognigy-service-state-manager
type: Opaque
data:
    # The full mongodb connection string, has a schema of:
    # mongodb://service-state-manager:<password>@<host>:<port>/xx or different for replica-set
    connection-string:

    # use 64 bytes of random value, hex
    # db-password: 