apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: live-agent-ui
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: live-agent-ui
    http:
      paths:
      - backend:
          service:
            name: live-agent-ui
            port:
              number: 8000
        path: /
        pathType: Prefix